// ❌ Nomes ruins, confusos ou ambíguos
const active = true
const user = false
const verified = true
const has = false
const done = true

// ❌ Código usando essas variáveis (sem clareza)
if (active) {
  // O que significa "active"? Ativo em quê?
}

if (!user) {
  // O que significa "user" ser falso? O usuário existe? Está logado? 
}

if (verified) {
  // Verificado onde? Email, conta, identidade?
}
