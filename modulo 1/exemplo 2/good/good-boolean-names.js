// ✅ Nomes claros, semânticos e autoexplicativos
const isActive = true
const isUserLoggedIn = false
const isEmailVerified = true
const hasPermission = false
const isProcessComplete = true

// ✅ Código mais legível
if (isActive) {
  // O usuário está ativo
}

if (!isUserLoggedIn) {
  // O usuário não está logado
}

if (isEmailVerified) {
  // O email foi verificado
}

if (!hasPermission) {
  // O usuário não tem permissão
}

if (isProcessComplete) {
  // O processo foi concluído
}
